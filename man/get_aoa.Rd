% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derived.R
\name{get_aoa}
\alias{get_aoa}
\title{Get cached age-of-acquisition estimates}
\usage{
get_aoa(language = NULL, form = NULL, measure = NULL, version = "current")
}
\arguments{
\item{language}{An optional string specifying which language's estimates to
retrieve.}

\item{form}{An optional string specifying which form's estimates to
retrieve.}

\item{measure}{An optional string (\code{"produces"} or
\code{"understands"}) to filter by measure.}

\item{version}{A string specifying which version of the Wordbank dataset
to use, e.g. \code{"v1.2"} to pin a released version for reproducibility.
Defaults to \code{"current"}, the most recent release.}
}
\value{
A data frame with one row per instrument item and measure:
  \code{language}, \code{form}, \code{item_id}, \code{item_definition},
  \code{category}, \code{uni_lemma}, \code{measure}, \code{aoa}.
}
\description{
Age-of-acquisition estimates for every word item on every instrument,
precomputed with \code{\link{fit_aoa}} (glm method, 50% threshold) at each
data release. \code{aoa} is \code{NA} for items that do not reach the
threshold within the instrument's age range.
}
\examples{
\dontrun{
danish_aoa <- get_aoa(language = "Danish", form = "WS")
}
}
